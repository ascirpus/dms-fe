# =============================================================================
# DMS Frontend — Deploy Configuration
# =============================================================================
# Copy template and fill in values:  cp deploy.conf.example deploy.conf
# Used by ./deploy.sh for manual CLI deployments.
# =============================================================================

# ── Required ───────────────────────────────────────────────────────────────
DROPLET_IP=1.2.3.4

# ── Optional ───────────────────────────────────────────────────────────────
# Defaults to "deploy"
# DROPLET_USER=deploy

# Leave empty to skip deploy notifications
# DISCORD_WEBHOOK_URL=
