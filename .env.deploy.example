# =============================================================================
# DMS Frontend Manual Deploy — Environment Variables
# =============================================================================
# Copy to .env.deploy and fill in values:  cp .env.deploy.example .env.deploy
# Used by ./deploy.sh for manual CLI deployments.
# =============================================================================

# ── Required ───────────────────────────────────────────────────────────────
DROPLET_IP=1.2.3.4

# ── Optional ───────────────────────────────────────────────────────────────
# Defaults to "deploy"
# DROPLET_USER=deploy

# Auto-detected from doctl if not set
# DOCR_REGISTRY_NAME=your-registry-name

# Leave empty to skip deploy notifications
# DISCORD_WEBHOOK_URL=
